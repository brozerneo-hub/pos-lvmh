# ── Variables d'environnement PRODUCTION (Render) ──────────────────────────
# Copier ces clés dans le dashboard Render → Environment

NODE_ENV=production
PORT=3001

# JWT — générer avec : openssl rand -base64 48
JWT_ACCESS_SECRET=REMPLACER_PAR_UN_SECRET_FORT_MIN_32_CHARS
JWT_REFRESH_SECRET=REMPLACER_PAR_UN_SECRET_FORT_MIN_32_CHARS

# CORS — URL exacte du frontend OVH (sans slash final)
CORS_ORIGIN=https://boomerintech.com

# Firebase — coller le contenu du fichier serviceAccountKey.json sur UNE SEULE LIGNE
# Exemple : {"type":"service_account","project_id":"pos-lvmh-prod",...}
FIREBASE_SERVICE_ACCOUNT=COLLER_LE_JSON_DU_SERVICE_ACCOUNT_ICI
